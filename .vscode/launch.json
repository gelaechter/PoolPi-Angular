{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Debug Angular",
            "request": "launch",
            "command": "npm start",
            "cwd": "${workspaceFolder}"
        }
    ]
}
